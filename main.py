from pandasmemuc import MeMuc
alldevices = MeMuc()

# alldevices.patch_hosts(vms_aa_index=alldevices.aa_index.to_list(), timeout=120)
# alldevices.bb_reboot()
# alldevices.bb_reboot.iloc[0] or alldevices.bb_reboot.apply(lambda x:x())